x = container.x;
y = container.y;
// Don't use? Why would I want the enemy to hit you through a wall?

/*
if(place_meeting(x, y, obj_guy))
{
	hurt_player(point_direction(xprevious, yprevious, x, y), container.attack_force, 1);
}
*/